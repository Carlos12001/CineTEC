/* The code is defining an interface called `Room` in TypeScript. An interface is a way to define the structure of an object. */
export interface Room {
  id: string;
  rows: number;
  columns: number;
  theatername: string;
  projectionid: string[];
}
